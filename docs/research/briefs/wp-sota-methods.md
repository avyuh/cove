You are a security researcher surveying the STATE OF THE ART for protecting API keys / credentials from being stolen or misused by a program you run locally — especially untrusted AI coding agents that execute arbitrary code. Use web search aggressively (today is 2026-07-05). We are building a tool and want to know every serious method used in industry, with honest pros/cons — we are not the first to solve this.

Survey these method families. For EACH: how it works, exactly what it protects against (theft-at-rest? exfiltration? misuse?), pros, cons, real-world adopters/products, and — critical — whether it COMPOSES with a "local namespace box + host-side egress/inject proxy" design.

1. CREDENTIAL BROKER / PROXY INJECTION — the credential lives outside the workload; a broker/proxy injects it at request time. Cover: Docker Sandboxes credential proxy, cloud instance metadata services (AWS IMDSv2, GCP/Azure metadata), HashiCorp Vault agent/sidecar, CyberArk Conjur / Secretless Broker, ssh-agent forwarding as the archetype. Note the injection point (header add vs MITM vs env vs socket).
2. EGRESS / NETWORK CONTROL — deny-by-default network so a stolen key can't be sent anywhere. Cover: sandbox network allowlists (Codex/Claude native), egress firewalls, service mesh policy.
3. SHORT-LIVED & SCOPED CREDENTIALS — reduce blast radius instead of hiding the key. Cover: OAuth device/refresh flows, AWS STS AssumeRole / IAM Roles Anywhere, GitHub App installation tokens, fine-grained PATs, SPIFFE/SPIRE workload identity / X.509 SVIDs, OIDC workload federation.
4. OS / HARDWARE KEYSTORES — Keychain, Secret Service/kwallet, TPM, HSM, hardware tokens, OS credential managers.
5. SANDBOXING — containers, gVisor, microVMs, the vendors' native agent sandboxes (bubblewrap/Landlock/seccomp) — as they pertain to key safety specifically.
6. REQUEST SIGNING (why it resists proxy injection) — AWS SigV4, HTTP Message Signatures, mTLS client certs: the client must hold the secret to sign/authenticate, so a header-injecting proxy CANNOT keep the key out. Explain precisely why, and what the only options are for such creds (keep local + contain egress, or use short-lived scoped variants).
7. EMERGING AGENT-SPECIFIC PATTERNS (2025-2026) — anything new specifically for AI agents / MCP (e.g. credential-less MCP, per-tool OAuth scoping, the GitHub-MCP repo-scoping response, policy/approval engines for destructive actions).

OUTPUT: a comparison table [method family | how it works | protects against | pros | cons | composes with local box+proxy?], then a ranked bottom line: which methods are SOTA for protecting keys from a COMPROMISED LOCAL AGENT, which are complementary, and which fundamentally cannot keep a key out of the process (so egress-containment is the only backstop). Cite URLs.