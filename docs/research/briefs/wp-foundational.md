You are a Linux systems engineer running a feasibility spike. QUESTION: can a single static binary provide "run an agent in a locked box" using ONLY Linux kernel namespaces — no podman, no bubblewrap, no crun, no nsjail — depending on nothing but a modern kernel? The box's ONLY job: (a) the agent process cannot see host secret files (they are ABSENT, not hidden), and (b) the agent has NO network egress except through a proxy we control. Be empirical: prototype it, report what works unprivileged and what it costs.

THE BOX: ARM64 Ubuntu 24.04, kernel 6.8, KVM guest, no /dev/kvm, 4c/7.5GB. Facts from prior spikes: kernel.unprivileged_userns_clone=1, user.max_user_namespaces=30630, BUT kernel.apparmor_restrict_unprivileged_userns=1 (this blocked raw bubblewrap earlier; we fixed bwrap with a per-binary AppArmor profile granting `userns`). subuid/subgid: dev:100000:65536 present. newuidmap/newgidmap setuid helpers available (uidmap pkg). podman works rootless. pasta + slirp4netns installed. You have passwordless sudo but the GOAL is an unprivileged-at-runtime design; note anything requiring root or setuid. Log persistent changes to /tmp/foundational-changes.txt.

PROTOTYPE (Go strongly preferred — the real tool will be Go; C acceptable to prove a point). Build a minimal `lockbox` that runs a command in:
1. USER NAMESPACE — unprivileged. CRITICAL: does an arbitrary Go binary calling unshare(CLONE_NEWUSER) get blocked by apparmor_restrict_unprivileged_userns=1 the way bwrap did? Determine exactly how podman/newuidmap get userns permitted here and whether our own binary can (a) map only the single current uid via /proc/self/uid_map without any setuid helper, or (b) needs newuidmap (setuid) for a range, or (c) needs a per-binary AppArmor profile. Report the minimal requirement — this decides whether "kernel-only, no deps" is actually achievable on stock Ubuntu 24.04 or needs an AppArmor profile install as a setup step.
2. MOUNT NAMESPACE — present a filesystem view where the agent has its projects + the tools it needs but host secrets are ABSENT. Two approaches, test both: (a) deny-by-default: build a new root by bind-mounting only /usr,/bin,/lib,/etc(curated),the projects dir — nothing else; (b) reuse host / read-only then mask specific secret paths (~/.ssh, ~/.aws, ~/.claude, browser profiles, ~/.config/gh, ssh-agent socket) with empty tmpfs/over-mounts. Which is more robust and simpler? Confirm the agent genuinely cannot read a host secret file placed as bait.
3. NETWORK NAMESPACE — the agent has NO usable network except a channel to our proxy. Test the cleanest mechanism: a new netns with only loopback (no veth, no external route), and give the child its ONLY egress via either (i) a preconnected socket / fd passed to the proxy, or (ii) a unix socket bind-mounted in with the proxy on the other side, or (iii) a TUN device our process owns. Demonstrate that a program which IGNORES HTTP_PROXY (e.g. a raw socket connect to 1.1.1.1:443, or curl --noproxy) CANNOT reach the internet — egress is deny-by-default and unbypassable.

OUTPUT: A verdict — is a zero-dependency (kernel-only) single-binary lockbox FEASIBLE unprivileged on this exact box? Exactly what does it require (nothing? an AppArmor profile install? a setuid newuidmap? root once at setup?)? What does it GIVE UP versus podman (no prebuilt polyglot image → the agent uses the host's own installed toolchain; lifecycle/exec management we'd write ourselves; is that a problem for 30 concurrent agent sessions?). Include the working prototype code and the exact commands proving points 1-3. Then a recommendation: kernel-namespaces-direct vs podman, for a tool whose ONLY job is credential protection. Be honest about the AppArmor/setup-friction reality.