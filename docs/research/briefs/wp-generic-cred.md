You are a security engineer investigating the MOST GENERIC way to protect credentials for ARBITRARY command-line AI agents on Linux — keyed by what protects a key, NOT by hardcoding each CLI. Be empirical: prototype and report. Today is 2026-07-05.

CONTEXT: A tool ("cove") runs YOLO coding agents (claude/codex/kimi and potentially any future CLI) in an isolated box. The mission is narrow: a compromised agent must not be able to STEAL/EXFILTRATE the user's credentials, and ideally the highest-value keys should never be in the box at all. The current design hardcodes per-CLI injection (claude honors ANTHROPIC_BASE_URL; codex is ChatGPT-OAuth and can't be base-URL'd). The owner wants a GENERIC mechanism so we don't hardcode each CLI unless truly required. Rootless podman is available; a netns/proxy over a unix socket is proven to work (claude inject PASS, codex egress PASS, wss traverses CONNECT).

INVESTIGATE TWO GENERIC MECHANISMS, keyed by DESTINATION HOST not by CLI:

PART 1 — TRANSPARENT, UNBYPASSABLE EGRESS ALLOWLIST (generic exfil protection for ALL tokens/CLIs).
The generic containment: force ALL of the box's network through our filter regardless of whether a program honors HTTP_PROXY. Prototype within a network namespace (use a manual netns or podman's netns): use nftables/iptables REDIRECT/TPROXY to send all outbound TCP to a local transparent proxy that allows by SNI/destination host and denies the rest; OR a TUN device + userspace forwarding. DEMONSTRATE that a program ignoring HTTP_PROXY (raw socket to an IP, curl --noproxy, a DNS query) is still captured and an off-allowlist destination is BLOCKED, while an allowlisted host works. This mechanism is CLI-agnostic by construction — confirm it. Report robustness + complexity.

PART 2 — GENERIC CREDENTIAL INJECTION BY HOST (keep keys out of the box for ANY CLI hitting a known API host).
Prototype a transparent MITM proxy: our own CA cert trusted inside the box, the proxy terminates TLS for CONFIGURED API hosts and injects the real Authorization header, so the box holds only a dummy/no key. Test with the real claude CLI hitting api.anthropic.com via transparent MITM (NOT via ANTHROPIC_BASE_URL — the point is to work generically without per-CLI env config): does claude accept our CA / does it cert-pin and refuse? Then assess codex: its ChatGPT-OAuth session token to chatgpt.com — can that even be injected generically, or is it fundamentally a session token that must live in the box? Determine: (a) does generic host-keyed MITM injection actually work for simple bearer/api-key auth across CLIs, (b) what breaks it (cert pinning, mTLS, OAuth session tokens, non-HTTP protocols), (c) is it meaningfully MORE generic than the per-CLI base-URL approach or just more fragile?

OUTPUT: For each part, a working verdict with evidence (captured connections, blocked/allowed proof, whether claude/codex accept MITM). Then a synthesis: is there a genuinely GENERIC credential-protection mechanism, and what is the honest split — e.g. "transparent egress allowlist is fully generic and unbypassable and protects every token from exfil; credential *injection* (keeping a key fully out) works generically via MITM only for non-pinning simple-bearer APIs and cannot cover OAuth-session CLIs, so injection stays targeted." Give a clear recommendation on generic-vs-hardcoded and what complexity it costs. Cite any relevant docs/URLs on CLI cert-pinning behavior.