You are a systems engineer running a MINIMAL, cheap capability probe to map what agent-isolation options are actually possible on THIS specific machine. Keep it minimal: NO building of large images, NO running real AI agents, NO heavy benchmarks. Cheap capability tests only. LOG every package you install to /tmp/probe-installs.txt so it can be rolled back. You have passwordless sudo.

THE MACHINE: ARM64 (aarch64) Linux, a KVM guest with 4 cores / 7.5GB RAM, NO /dev/kvm, no nested virt. Currently installed: claude, codex, tmux, git, gh. NOT installed: podman, docker, go, runsc, bubblewrap. This is the box on which the owner runs Claude Code / Codex coding agents in YOLO (--dangerously-skip-permissions / full-access) mode and wants low-friction containment that protects the host.

GOAL: Produce a feasibility matrix of isolation primitives so a later design pass can choose. The owner's explicit questions to answer:
1. MAKE-OR-BUY: Does Docker `sbx` (docker sandboxes) actually work on THIS hardware, or does its microVM backend require /dev/kvm (which is absent)? Research its requirements precisely (docs + `docker sbx` needs). Do NOT necessarily install Docker — determine from docs + a capability check whether it COULD run here at all. Same quick check for whether container-use (Dagger) or Imbue mngr would run rootless here. Bottom line: can he just BUY (use an existing tool) instead of building?
2. ROOTLESS PODMAN: install it (apt), confirm rootless works with a tiny image (e.g. `podman run --rm alpine echo ok` or busybox). Confirm `--userns=keep-id`, subuid/subgid present. Note setup friction.
3. gVisor / runsc: install runsc, try `podman run --runtime runsc --rm alpine echo ok` rootless with the systrap platform (no KVM). Does it work on this ARM64 kernel unprivileged? Capture exact errors if not. This is the SPEC-REVIEW spike #5 — settle it cheaply.
4. BUBBLEWRAP: install bwrap, test an unprivileged namespace sandbox: bind-mount a workdir, `--unshare-all` (or --unshare-user-try + net), run a shell command inside, confirm the host is isolated but the process runs unrestricted INSIDE (no seccomp/landlock layered on — the point is low friction). Assess: could bwrap alone (namespaces + bind mounts + a net namespace) be a lighter-weight isolation primitive than podman for this use case?
5. KERNEL CAPABILITIES: check and report: /dev/kvm (confirm absent), unprivileged_userns_clone / user.max_user_namespaces, cgroups v2, seccomp support, subuid/subgid ranges, whether unprivileged network namespaces + pasta/slirp4netns are available (for egress-allowlist feasibility under rootless).

ALSO briefly assess for each working option: (a) approximate RAM overhead per sandbox (from docs/measurement if trivial), (b) whether it supports MANY parallel sandboxes cheaply on 7.5GB, (c) whether a SINGLE SHARED sandbox hosting multiple agent sessions is natural vs per-agent, (d) rootless network egress-allowlist feasibility (the real anti-exfil control).

OUTPUT to stdout (will be captured): a matrix table [Primitive | Runs here? | Rootless? | Needs KVM? | Setup friction | YOLO-inside friction | Host protection | RAM/parallelism | Egress-allowlist feasible?] followed by: a clear bottom-line on make-vs-buy (can an existing tool just be used?), the single best-supported primitive on this box, and any hard blockers found. Cite doc URLs where you relied on docs rather than tests. Be concise and concrete. Remember: minimal spike — do not overbuild.