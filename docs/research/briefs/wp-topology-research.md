You are a researcher investigating how power users actually run MANY parallel AI coding agents, and what topology they want. Use web search aggressively (today is 2026-07-05). The goal is to ground a design decision in real practice + desire, not theory. Bias toward concrete evidence (specific posts, repos, quotes), and report honestly even if it contradicts the premise.

CONTEXT: A developer runs up to ~30 concurrent agent sessions (Claude Code, Codex, kimi) in full YOLO mode across 10+ projects (5-7 active) on a single Linux VPS. He's deciding the "object model" for a containment tool: should there be ONE sandbox for everything, ONE sandbox per project (agents in a project share it), one sandbox per agent, or a flexible m:n mapping? He questions whether isolating agents from EACH OTHER (same user, same creds, same trust) protects against anything at all.

RESEARCH QUESTIONS:
1. TOPOLOGY IN PRACTICE: How do people who run many parallel coding agents actually structure it? Search HN, Reddit (r/ClaudeAI, r/ChatGPTCoding, r/selfhosted, r/LocalLLaMA), X, blog posts, and tool docs. Do they use one shared environment (one machine/container, many tmux panes/agents), git worktrees in a shared checkout, one container per task, one VM per project, Dev Containers, etc.? What's the DOMINANT pattern for high-parallelism users? Cite specifics.
2. SHARED vs ISOLATED — WHAT DO THEY WANT: Do heavy users express a desire to ISOLATE agents/projects from each other, or do they run everything together and value the convenience? What specifically do they complain about (resource cost of isolation? blast radius fears? lost shared state?)? Look at how claude-squad, container-use, worktree-based tools, uzi, mux, mngr etc. structure it and what users say about that structure.
3. THE UNIT OF ISOLATION: When people DO isolate, what's the unit — per-task, per-project/repo, per-agent, per-branch/worktree? Is per-agent isolation a thing anyone asks for, or is it always per-task/per-project?
4. RESOURCE PATTERNS: For someone running ~30 agents on a small box, what do people say about the RAM/overhead of per-task containers vs shared? Any reports of container-per-agent being too heavy, driving people to shared setups?

OUTPUT: A tight, cited memo: the dominant topology pattern(s) for high-parallelism agent users; whether isolation between agents/projects is something users actually want or avoid; the real unit of isolation people use; and a one-paragraph bottom line: for a single-user 30-session/10-project setup, what object model does the evidence favor (one global sandbox / per-project / per-agent / flexible)? Be concrete and honest; cite URLs.