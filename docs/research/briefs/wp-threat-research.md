You are a security researcher grounding a threat-model question in real-world evidence. Use web search aggressively (today is 2026-07-05). Report honestly, including if the feared threats turn out to be rare or theoretical.

CONTEXT: A single developer runs up to ~30 YOLO-mode AI coding agents (Claude Code, Codex, kimi — all HIS, same credentials, same trust) across 10+ projects on one Linux VPS. A containment tool will add: (a) a host boundary (rootless container), and (b) a credential/egress proxy (agents never hold raw API/OAuth tokens; deny-by-default network allowlist). The open question: beyond host-protection and credential/egress control, does he need to isolate agents or projects FROM EACH OTHER? What is the actual, evidenced threat?

RESEARCH QUESTIONS:
1. WHAT ACTUALLY GOES WRONG with YOLO/auto-approve coding agents? Find real incidents/reports (2025-2026): prompt injection via dependencies/READMEs/web content, data exfiltration, agents deleting/corrupting files, running destructive commands, leaking secrets. How common, how severe, what was the vector? (e.g. the known cases of agents wiping data, the Claude Code / Codex sandbox-escape or injection discussions, supply-chain via npm/pip.)
2. LATERAL MOVEMENT / CROSS-PROJECT: Is there real evidence of one compromised agent affecting OTHER projects or OTHER agents on the same machine? Or is the documented harm almost always contained to the single task/repo the agent was working on + credential exfiltration + host damage? Does anyone report needing project-to-project isolation specifically?
3. CREDENTIAL/EGRESS AS THE PRIMARY CONTROL: Does the evidence support the claim that "voluntarily-granted secrets + open egress" is the dominant real risk (vs kernel escape or agent-to-agent)? What do security analyses of agentic coding actually prioritize?
4. THE SHARED-CREDENTIAL POINT: If all agents share the same gh token + same Claude/Codex subscription via a proxy, does isolating them from each other reduce ANY meaningful risk (they already have equal credential access)? Reason about this and find any supporting/contradicting discussion.

OUTPUT: A tight, cited memo: the real, evidenced threat model for a single-user many-agent YOLO setup, ranked by actual severity/frequency; a clear verdict on whether project-to-project and agent-to-agent isolation buy meaningful risk reduction beyond the host boundary + credential/egress proxy; and a one-paragraph bottom line on what the containment MUST do vs what is security theater. Cite URLs. Be honest if agent-to-agent isolation is pointless.